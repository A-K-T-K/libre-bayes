import { tokens } from "@fluentui/react-components";

// A vivid, distinct-hue categorical palette (cycles by state index), shared
// between a node's own bar-chart rendering and the Inspector's temporal
// trajectory plot so the same state reads as the same color everywhere.
export const BAR_PALETTE = [
  tokens.colorPaletteBlueForeground2,
  tokens.colorPaletteMarigoldForeground2,
  tokens.colorPaletteBerryForeground2,
  tokens.colorPaletteTealForeground2,
  tokens.colorPaletteGrapeForeground2,
  tokens.colorPalettePumpkinForeground2,
  tokens.colorPaletteCranberryForeground2,
  tokens.colorPaletteGreenForeground2,
];
